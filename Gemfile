source 'https://rubygems.org'

gemspec

# Ruby Curses standard library for Rubinus
gem 'rubysl-curses', platform: :rbx

group :development do 
  gem 'pry'
  gem 'guard-rspec', require: false
  gem 'terminal-notifier-guard' if RUBY_PLATFORM =~ /darwin/
end